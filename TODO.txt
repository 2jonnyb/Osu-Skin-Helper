MUST HAVES
=wire select buttons to dialog
=wire select output location
~skins.ini filedialog and read/write
#wire tick buttons for gamemodes
~completion process
  =create folder for skin
  =add skin.ini
  =populate with copies of images
  -create downscaled SD version of each element
  -resize to appropriate size

-skin folder selection

-audio
-handle numbered elements
-create header labels

~element browser
  =resize maintain aspect ratio
  -numbered elements
  =dynamic sd/hd (show highest avaiable and choose appropriate)
  =wire to main window

NICE TO HAVES
-dynamic window
-icon
-existing gallery
-advanced skin.ini setup
-smart element selector
-choose skin in progress, fill completed items
-null element, mainly for audio files. a blank file that overwrites the default skin so there is no element
